#include <bits/stdc++.h>

using namespace std;

void solve() {
  int n;
  cin >> n;
  cin >> n;
  vector<pair<int, int>> ar(n);
  for (int i = 0; i < n; i++) {
    cin >> ar[i].first >> ar[i].second;
  }

  int lo = 1;
  int hi = 
}

int main() {
  int tc;
  cin >> tc;

  for (int t = 0; t < tc; ++tc) {
    solve();
  }
  return 0;
}